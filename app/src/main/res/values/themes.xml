<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.light" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/teal_200</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?colorOnBackground</item>
        <!-- Customize your theme here. -->
        <item name="android:layout_height">@dimen/dimen_100</item>
        <item name="android:layout_width">match_parent</item>
        <item name="background">@color/white</item>
        <item name="background_color">#ffff</item>
        <!--        Our app's background colour when light mode is on == white-->
        <item name="text_color">#000</item>
        <!--        Text color is black-->
        <item name="button_color">#000</item>
        <item name="android:windowContentTransitions">true</item>
    </style>
    <style name="ThemeOverlay.SearchView" parent="Theme.AppCompat.NoActionBar">
        <!-- Text color -->
        <item name="android:editTextColor">@color/black</item>
        <!-- Hint text color -->
        <item name="android:textColorHint">@color/white</item>
        <item name="android:borderlessButtonStyle">@color/grey</item>
    </style>
    <style name="toolbarMenuTheme" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/dimen_20</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>